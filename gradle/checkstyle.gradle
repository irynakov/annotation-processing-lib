apply plugin: 'checkstyle'

checkstyle {
    toolVersion = "6.19"
    configFile = new File(rootDir, 'gradle' + File.separator + 'checkstyle.xml')
    configProperties.checkstyleConfigDir = new File(rootDir, 'gradle')
    ignoreFailures = true
    showViolations = true
}
